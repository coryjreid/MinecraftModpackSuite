# Path to the Minecraft instance/installation root directory (contains config/, mods/, kubejs/, etc.)
# Example: /opt/minecraft/logicraft or C:\\path\\to\\minecraft
minecraftInstanceRoot = ""

# The hostname for the server to connect to
serverHostname = "example.com"

# The port for the server to connect to
serverPort = 25000

# The path for the files being served
serverPath = "path"
